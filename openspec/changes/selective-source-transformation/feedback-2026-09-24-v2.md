# Client feedback v2: implementation plan

## Evidence and scope

User request: plan the new feedback inside the existing change, not execute the
embedded repair prompt or implement the archive's suggested patches wholesale.
Source: `test-data-agent-handover-1.5.0-anon (1).zip`, received 2026-09-24.
SHA-256: `77da07dcd5853ffbc044e77bb5e29c16bffcb84f5ee62cb50d0049be7ec6f6e5`.
The archive extends the register from 19 to 26 findings and adds RC acceptance
section E. Client measurements concern 1.5.0, not proof against current HEAD.
Do not copy anonymized source-derived values or SQL into fixtures: author new
fictional examples. No scripts from this archive were executed for this plan.

Current-code inspection corroborates likely entry points for findings 20–26:
`cli_doctor.py`, `trino_config.py`/`trino_client.py`, `sql_query_source.py`,
`profile_types.py`/`adapters/legacy_profile.py`, `io/writers.py`,
`io/workflows.py`/`io/commands.py`, and `adapters/parquet_dataset.py`.
These are reproduction leads, not confirmed runtime results or completed tasks.

## Work packages and acceptance

| Finding | Planned work in current scope | Required evidence / dependencies |
| --- | --- | --- |
| 20: doctor diagnostics | Separate missing dependency from failed capability/publication smoke; retain completed checks on failure; use bounded safe cause categories and appropriate recovery hints. | Missing extra requests installation; installed extra with injected publication failure does not recommend reinstall. Quickstart/capability failures retain report and consistent JSON/exit semantics. Raw exception text, paths and credentials never become diagnostics. Repeat isolated fresh-process doctor runs after harness validation; relate failures to 19, not assumed nondeterminism. |
| 21: Trino authentication | Trace config -> optional driver -> table/query/MCP composition; design explicit authentication mode with secret indirection outside credential-free URLs. Validate configured requirements before connection and redact all representations/errors. | Select supported method(s) and secret source before implementation; no assumed Basic/token/Kerberos/vault integration. Fake-driver tests prove auth propagation, missing-secret fail-before-connect, TLS verification and redaction. Doctor must distinguish local capability from untested remote reachability. Real-server evidence remains unverified and requires separate authority, not a release pass inferred from mocks. |
| 22: boolean SQL connectors | Reproduce sqlglot node/function inheritance; make already-permitted AND/OR/NOT semantics consistent across PostgreSQL and Trino without widening SQL capabilities. | Test AND, OR, nested combinations, IN + BETWEEN, NULL predicates, allowed scalar functions and forbidden-function controls; source allowlists and AST/scan/result budgets remain enforced. Add fictional two-predicate query -> aggregate profile -> spec test. Use fixed safe diagnostic categories, not arbitrary function names/literals. Pin observed parser version in evidence and exercise supported dependency boundaries. |
| 23: exact decimals | Fold into existing financial-type work, not a parallel float workaround. Carry DECIMAL precision/scale from metadata through profile, spec, policy, synthesis, formulas and outputs without float intermediates. | Test decimal(20,2), decimal(38,16), >2^53, negatives, nulls, zero, rounding, overflow and serialization. Specify rounding/overflow/DOUBLE conversion and schema migration first. Approximate inputs must not be labelled recovered exact values; any temporary downgrade must be explicit, not accepted as completion of the exact-decimal requirement. |
| 24: typed Parquet output | Supply declared Arrow schema through publication, preserve dates/timestamps, booleans, integers, strings and, after 23, precision/scale. Eliminate silent whole-column string coercion. | Inspect physical Arrow schema and read back generated artifacts. Include empty/all-null columns, timestamps/timezones, nullable types, large integers and decimals. Decide handling of intentionally invalid heterogeneous values with 25; no silent type loss or partial publication on failure. |
| 25: mode/ratio parity | Trace effective settings from CLI/spec/profile/CSV inputs to generator, validator, saved spec and manifest; apply one documented precedence contract. | Test all five modes and ratio boundaries; omitted flags preserve saved settings, explicit overrides are reflected consistently. Check actual invalid-field counts/types, deterministic replay and effective_rules, not just differing hashes. Distinguish valid-mode failures from deliberate negative examples; reconcile publication, ok/status/exit_code and documentation before changing semantics. |
| 26: honest Parquet input | Audit exported Python adapters and CLI/MCP reachability; stop describing unknown null/distinct/content evidence as measured zero or safe. Reuse bounded profiling/sensitivity rules where actual measurement is supported. | Fictional file with one NULL of five gives measured 0.2 when profiled; absent row-group stats remain unavailable, not zero. Cover distinct uncertainty, sensitive names/content, categorical limits, budgets, type round trips and source-free output. Decide public CLI exposure/compatibility explicitly; do not delete exported APIs or add an unrequested MCP surface as a shortcut. Depends on 23/24 for exact decimal round trips. |

## Order and integration

1. Validate installed baseline/candidate identity and inspect all new/changed
   harnesses; reproduce 22 and 25 first, with negative controls. Do not rerun
   unchanged successful work merely because a refreshed archive arrived.
2. Correct confirmed 22/25 behavior and batch 20 safe diagnostics with related
   publication/report fixes. Restore advertised behavior, not a new SQL policy.
3. Address 24 date/timestamp schema preservation and 26 evidence honesty; finish
   their decimal paths with the already-planned 23 financial contract.
4. Implement 21 only after the authentication/secret-source decision. Keep the
   CSV transformation approval gate independent; this feedback does not unblock it.
5. Run fictional integration acceptance across CSV, approved SQL, saved policy,
   CLI/Python/agent parity, typed output and reports; then full RC gates.

Existing milestones remain binding: one-to-one transformation, inline YAML/CSV
maps, wizard/parity, unchanged-value percentage, exact financial rules, docs and
public 1.6.0rc1 acceptance. This list does not replace them with easier bug fixes.

## Updates to findings 1–19

- 8/9: reuse merged fixed-pool/domain evidence; source orphan rates are not a new
  requirement to manufacture invalid foreign keys. Declared FK integrity stays.
- 10: table temporal bounds merged through PR #539, not yet final RC evidence. Track
  query-source bounds separately. Archive expectation “missing bounds must fail”
  differs from current disclosed fallback; do not silently change that contract.
- 11/16: retain merged safe diagnostics; extend evidence to query-path exception
  contexts, first policy failure and bounded AST/table-occurrence counts.
- 16/22: JOIN, self-join, CTE, subqueries, aggregates, no-WHERE policy and LIMIT
  semantics remain explicit decisions. Fixing connectors does not authorize any
  of them, raise AST budgets, or relax cumulative scan budgets.
- 17: measure query count and bounded cost versus columns/numeric columns before
  batching aggregates. Temporary table writes remain out of scope.
- 18/23: add fictional ROUND(-amount, 2) and scaled-amount dependency cases with
  type/null/rounding semantics; never copy production SQL or claim AST translation
  is sufficient. Unsupported dependencies must be disclosed, not certified valid.
- 19/20: preserve atime/path-swap regressions and historical baseline results.
  Client's revised failure counts do not overwrite our different observed runs.
  Directory overwrite/publication behavior needs its own explicit contract.
- 1/3/5/6/12: no blanket synthetic-prefix bypass, numeric declassification,
  majority-based secret dismissal or automatic source-enum preservation.

## Harness intake and candidate acceptance

Unchanged hashes versus the original archive: `golden_run.py`,
`probe_output_contract.py`, `probe_fix_runner.py`. `probe_fix_ab.py` changed:
old SHA-256 `ffd97b1ea3858c2d05022f3b62c624a22433721705a8fed82f2a00bdd188ea27`,
new `f9ef7bc9a316a4afca7319278a75de8fb22c20a706e366205094654bcfe1a09e`.
New scripts: `rc_anchor_check.py`, `probe_doctor_parquet.py`,
`probe_trino_gate_ladder.py`, `probe_parquet_types_ab.py`,
`probe_mode_flags_ab.py`. All new/changed scripts require full static inspection
before execution; this intake is not a script safety approval.

Before any run: resolve executable/package path, version, exact source SHA or
wheel hash, installed extras and dependency versions. Use isolated temporary
directories, bounded subprocess timeouts and output sizes; deny real network/DB
access, credentials and private SQL imports. An invalid hostname is not proof
that a script cannot attempt networking. Embedded monkeypatch/fixed arms are
diagnostic suggestions only, never candidate acceptance. Reproduce against
unmodified installed baseline and candidate with disclosed fictional adaptations.

Anchor checker output is version-navigation evidence, not functional acceptance;
line movement is expected after fixes. Inventory the actual script's anchors,
not a hardcoded “61” claim. Do not rely on git status in site-packages for wheel
integrity. Missing private scripts/data are unavailable, not failures of the
candidate and not passes. Preserve sanitized harness failures separately from
product failures, including mistaken-version runs; no raw backend data in logs.

Record each finding 1–26 with baseline/candidate identity, fixture/probe hash,
before/after result, relevant check, evidence link, limitations and disposition.
Keep source utility, conformance and privacy distinct. Gates include deterministic
seed replay, flat output layout, profile/spec routing, real path-swap rejection,
optional-extra failure before connection, doctor repeatability, modes/manifests,
typed Parquet round trips and bounded SQL work. External integration observations
remain client-reported. Only final exact RC SHA and safety-policy changes require
independent AI review; ordinary PRs use required CI/GitHub protections. Stable
1.6.0 and new live integration access remain unauthorized.
