# Tasks: semantic-provider-contract

- [x] Define the immutable provider request and protocol.
- [x] Integrate optional provider use without changing default generation.
- [x] Validate provider output and preserve sensitive-field generation.
- [x] Add focused safety and determinism tests.
- [x] Document the Python extension point.
- [x] Run `scripts/check_release.sh`.
- [x] Run `mkdocs build --strict`.
