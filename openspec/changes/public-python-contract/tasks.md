# Tasks: public-python-contract

- [x] Add a deterministic top-level Python API contract fixture.
- [x] Verify every contracted export resolves from the package.
- [x] Update roadmap and changelog.
- [x] Run `scripts/check_release.sh`.
- [x] Run `mkdocs build --strict`.
