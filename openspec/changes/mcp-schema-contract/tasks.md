# Tasks: mcp-schema-contract

- [x] Add deterministic generator MCP schema fixtures.
- [x] Add deterministic default Trino MCP schema fixtures.
- [x] Assert raw safe SELECT remains opt-in.
- [x] Update roadmap and changelog.
- [x] Run `scripts/check_release.sh`.
- [x] Run `mkdocs build --strict`.
