# Tasks: <change-id>

- [ ] Update or add affected OpenSpec capability requirements.
- [ ] Implement the smallest behavior change.
- [ ] Add unit tests for deterministic logic.
- [ ] Add integration or CLI/MCP tests for the user-visible workflow.
- [ ] Update README, changelog, and relevant docs.
- [ ] Run `python3 -m ruff check src tests`.
- [ ] Run `python3 -m compileall -q src tests`.
- [ ] Run `python3 -m pytest --cov=test_data_agent --cov-report=term-missing --cov-fail-under=85`.
